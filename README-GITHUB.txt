MY DAY — GitHub Pages / Offline package

Upload all files in this folder to the root of your GitHub repository and enable GitHub Pages.
The app is self-contained in index.html and includes an offline service worker.
Open the site once while online so the browser can cache the app shell.
Planner data is stored locally in the browser (localStorage) on the device/browser where you use it.
